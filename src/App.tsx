import { useState, useEffect } from "react";
import Header from "./components/header";
import "./App.css";

const App = () => {
  const [] = useState({});
  useEffect(() => {
  }, []);

  return (
    <div>

      <Header />
    </div>
  );
};

export default App;
